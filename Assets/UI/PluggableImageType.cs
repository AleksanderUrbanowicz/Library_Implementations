using UnityEngine;
namespace GeneralImplementations.UI
{
    public class PluggableImageType : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}